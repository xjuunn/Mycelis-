{
  "tasks": {
    "start": {
      "dependsOn": ["^build"],
      "outputs": [],
      "env":["JWT_SECRET","FILE_URL","BASE_URL"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    }
  }
}
